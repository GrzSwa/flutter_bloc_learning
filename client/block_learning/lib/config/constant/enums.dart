enum ConnectionType { connected, disconnected }

enum AuthenticationStatus { none, unatuhenticated, authenticated, loading }

enum Role { admin, constration, manager, serviceman, leader }

enum InputType { username, password, number }
